import setuptools

setuptools.setup(
    name="dschat",
    version="0.1",
    packages=setuptools.find_packages(include=['dschat', 'dschat.*']),
    install_requires=[
    ],
    entry_points={
        'console_scripts': [
        ],
    },
)